@(heading: String, subject: String, message: String, isCopy: Boolean = false)(implicit req: RequestHeader, globalConfig: global.GlobalConfig, lang: Lang)

@Messages(heading)

    @subject
    @((0 to subject.size).map(_ => "-"))

    @message
@if(!isCopy) {
@Messages("portal.mail.message.replyFooter"):

<a href="@controllers.portal.users.routes.UserProfiles.updateProfile.absoluteURL(secure = globalConfig.https)">
    @Messages("portal.profile.preferences.updatePreferences")
</a>
}