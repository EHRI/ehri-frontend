@(repository: Repository)(implicit lang: Lang)

<publicationstmt>
    <publisher encodinganalog="Publisher">@repository.toStringLang</publisher>
    @repository.model.primaryDescription.flatMap(_.addresses.headOption).map { add =>
        @address(add)
    }
    <date></date>
</publicationstmt>