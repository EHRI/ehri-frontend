@(title: String, repo: Repository, trees: Seq[utils.ead.DocTree])(implicit lang: Lang)

<eadheader>
    @trees.headOption.map(_.id).map { id =>
    <eadid>@id</eadid>
    }
    <filedesc>
        <titlestmt>
            <titleproper>
                @title
            </titleproper>
            <author>

            </author>
        </titlestmt>
        @publicationStatement(repo)
        @profileDesc(repo)
    </filedesc>
</eadheader>