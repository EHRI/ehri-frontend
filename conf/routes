# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Defer other routes to modules
-> /                    portal.Routes
-> /guides              guides.Routes

# Admin routes
-> /admin               admin.Routes
-> /admin/guides        guideadmin.Routes

# API routes
-> /api/v1              apiv1.Routes
