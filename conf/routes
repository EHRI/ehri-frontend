# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Map static resources from the /public folder to the /assets URL path
GET     /robots.txt                     controllers.Assets.at(path="/public", file="robots.txt")
GET     /assets/*file                   controllers.Assets.at(path="/public", file)

GET     /*path/                         controllers.GlobalController.untrail(path)

# Defer other routes to modules
-> /admin               core.Routes
-> /admin               admin.Routes
-> /admin/link          linking.Routes
-> /admin/annotation    annotation.Routes
-> /admin/sets          authorities.Routes
-> /admin/arch          archdesc.Routes
-> /admin/voc           vocabs.Routes
-> /guides              guides.Routes

# Portal is special
-> /                    portal.Routes
