# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Defer other routes to modules
-> /                    core.Routes
-> /                    portal.Routes

# Admin routes
-> /admin               admin.Routes
-> /admin               linking.Routes
-> /admin               annotation.Routes
-> /admin               authorities.Routes
-> /admin               archdesc.Routes
-> /admin               vocabs.Routes
-> /admin               guides.Routes
-> /admin               adminutils.Routes

