# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Map static resources from the /public folder to the /assets URL path
GET     /robots.txt                     controllers.Assets.at(path="/public", file="robots.txt")
GET     /assets/*file                   controllers.Assets.at(path="/public", file)

GET     /*path/                         controllers.GlobalController.untrail(path)

# Defer other routes to modules
-> /                    core.Routes
-> /                    portal.Routes

# Admin routes
-> /admin               admin.Routes
-> /admin               linking.Routes
-> /admin               annotation.Routes
-> /admin               authorities.Routes
-> /admin               archdesc.Routes
-> /admin               vocabs.Routes
-> /admin               adminutils.Routes
-> /guides              guides.Routes

